package com.example.test.ui

/**
 * Class WarningUiState represents the UI state for the SIGMETS Api
 * It holds a list of warnings, with each entry representing data as coordinates and an text.
 */
class WarningUiState(val warnings: List<Any>)